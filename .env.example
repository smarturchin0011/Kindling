# 这个文件是可选的 —— 日常使用不需要它。
#
# 推荐做法：启动服务后点右上角「设置」粘贴 API key。
# key 只存在内存里，不写入任何文件，重启需重新输入。
#
# 下面的环境变量主要给 CI / 自动化场景用。
# 若同时存在，应用内输入的 key 优先级更高。

# 可选：OpenRouter API key（https://openrouter.ai/keys）
# 注意：写在这里就意味着 key 落到了磁盘上。确认 .env 已被 .gitignore 忽略。
# OPENROUTER_API_KEY=sk-or-v1-你的key

# 可选：首次启动的默认模型。之后由应用内「设置」面板接管。
# KINDLING_MODEL=anthropic/claude-sonnet-4.5

# 可选：服务地址
# KINDLING_HOST=127.0.0.1
# KINDLING_PORT=8777

# 可选：状态文件位置（默认 ~/.kindling/state.json）
# KINDLING_STATE=/path/to/state.json
