# Kindling 项目交接 Prompt

> **用法**：把下面 `====` 之间的全部内容,作为新对话的第一条消息发给另一个 LLM agent。
> 它包含了继续这个 session 所需的全部上下文:用户画像、产品论证、架构决策、
> 已完成的工作、以及唯一真正重要的未完成事项。

---

====================== COPY FROM HERE ======================

# 交接上下文:Kindling 项目

你正在接手一个进行中的项目。以下是全部必要上下文。**请先读完再行动,不要急于写代码。**

## 1. 用户画像(重要,决定你的沟通方式)

- **INTJ,擅长宏观架构思考**。规划功能时会优先考虑顶层设计的解耦性、可扩展性、
  让一个功能兼容更多场景。
- **他的核心痛点**:这种思维导致**起步瘫痪** —— 越想周全越不知从哪开始。
- **沟通偏好**:
  - **用中文回复**
  - 要求**层层递进**地讲解架构(宏观→细节,一次一层,并给出下一层可选方向)
  - 重视**可迁移的设计经验**,而不只是"怎么做"
  - 讨厌含糊 —— 说了做到,做不到直说
- 代码仓库统一克隆到 `D:\Github_PROJECT`
- 环境:Windows 10,终端是 **git-bash (MSYS)**,不是 PowerShell。
  用 POSIX 语法(`ls`/`$HOME`/`&&`),路径可用 `/d/Github_PROJECT/...`
- Python:`python`=3.11.15(注意没有 `python3`),包管理用 **uv**,Node 22 可用

## 2. 项目是什么

**Kindling(火种)** —— 一个对抗"宏观思维者起步瘫痪"的上下文引擎。

- **仓库**: https://github.com/smarturchin0011/Kindling (**PUBLIC**)
- **本地**: `D:\Github_PROJECT\kindling`
- **当前 HEAD**: `0c17ac0`,本地与远端已同步
- **规模**: ~3160 行(后端 13 个模块 + 单文件前端 1359 行)
- **测试**: 114 passed,零网络依赖

### 核心命题(整个产品的地基)

> **上下文不是想出来的,是做出来的。**
> 所以行动不是上下文的下游产物 —— 行动是获取上下文的唯一手段。

**Move 的定义因此被重写:Move ≠ 任务。Move = 一个只能通过行动才能填补的上下文缺口。**

### 三个关键诊断(不要在新对话里推翻这些,它们是论证过的)

1. **用户的问题不是"思维混乱",是"思维过度有序"。**
   他的想法接得太好,形成一个自洽但无法启动的完美闭环。
   给混乱者结构=解除阻塞;给过度有序者结构=**加重病情**。

2. **LLM 输出的漂亮框架对他是麻醉剂,不是燃料。**
   框架会产生"认知闭合"的满足感,生理上近似"完成了任务",于是**替代行动**。
   这解释了为什么他之前用 ChatGPT 整理过却仍然没开始。

3. **用户自己的判断(部分正确)**:他认为"强 LLM + 足够上下文就能整理出结构,
   所以产品应主攻上下文问题"。
   —— **引擎判断对了(上下文是瓶颈),输出口judgment错了(不该是结构)。**
   "上下文不够"要拆成三层,价值密度差 10 倍:
   (a) 表达成本高 (b) 上下文腐烂/会话孤岛 (c) **他不知道自己缺什么上下文 ★最高价值**
   第 (c) 层是护城河:用户不知道缺什么,但 **LLM 知道**。
   所以交互方向必须反转:**不是用户提问 LLM 回答,而是 LLM 提问用户回答。**

## 3. 架构(六层 + 一个飞轮)

```
L1 采集      碎片输入,280 字上限,输入时 0 个结构决策
    ↓
L2 上下文层  ★真正的资产。带类型条目 + 权重
             证据5.0 > 约束3.0 > 事实2.0 > 意图1.0 > 偏好0.5
             (证据最贵,因为它是唯一不能靠"想"产生的类型)
    ↓ 读                                        ▲ 写入证据
L3 缺口层    ★护城河。LLM 计算"要给出只对你成立的答案,我最缺哪一条"  │
    ├── 能凭记忆答 → 追问一句 → 回 L2                              │
    └── 只能靠行动答 ↓                                            │
L5 收窄层    唯一一个 Move(≤5min,全局只允许 1 个 open)             │
    ↓ 用户真的做了                                                │
L6 回流层    产出物 → 证据条目 ───────────────────────────────────┘
    ↓
L4 综合层    完整度 ≥60% 才允许 → 2-3 个竞争框架
```

**飞轮(L6→L2)是全部的关键**:AI 笔记的上下文是静态的,Kindling 的是**复利**的。

### 四个硬机制(代码级强制,不是 UI 建议)

| 机制 | 实现 | 防止什么 |
|---|---|---|
| 降级承诺 | 条目 ≤280 字,输入时零分类决策 | 物理上无法在第一步写设计文档 |
| 竞争框架 | 一次必须 2-3 个,每个显式声明 `优化什么/牺牲什么` | 单方案引发"不够好我要改"的无底洞 |
| 完整度闸门 | <60% 时 synth 拒绝执行(HTTP 423) | 上下文薄时拿到漂亮空框架(麻醉剂) |
| **结构必须过收窄层** | **pick 后强制推导出 Move** | **认知闭合替代行动** |

第四条最重要:**看到漂亮结构在机制上不是一个可以停下来的状态。**

### 关键参数(改动前先想清楚)

```
GATE_THRESHOLD = 0.60      MIN_EVIDENCE = 2
MIN_CONSTRAINTS = 1        SATURATION = 20.0
```
`SATURATION=20` 是刻意与结构最小值对齐的:`2×5.0 + 1×3.0 = 13`,`13/20 = 65% > 60%`。
**不要随意改 SATURATION** —— 一旦"满足结构最小值"和"分数过阈值"不同时成立,
闸门会自相矛盾(提示缺证据,补齐了却仍打不开),彻底摧毁信任。
`tests/test_domain.py::test_gate_criteria_are_consistent` 锁住了这个不变量。

## 4. 技术栈与文件地图

FastAPI + 单文件 JSON 存储 + 零构建单文件 HTML 前端(手写 CSS,无框架无 CDN)。
**技术选型刻意"低级"**:最大风险不是技术风险,而是**产品自己变成完美主义陷阱**。
不用数据库/向量检索/前端框架 —— 30 条上下文用不上任何索引结构。

```
src/kindling/
  context.py         L2 带类型条目 ContextEntry + 权重 + 渲染顺序(权重降序)
  completeness.py    完整度评分 + 闸门(可解释,刻意不让 LLM 打分)
  gap.py             L3 缺口检测(护城河,含 SYSTEM prompt)
  moves.py           L5 Move(携带 retrieves_type/retrieves_why)
  reflux.py          L6 回流(飞轮闭合点)
  synth.py           L4 带闸门的竞争框架生成
  store.py           JSON 持久化 + 单 open Move 硬约束
  credentials.py     ★ API key 只存进程内存,绝不落盘
  models_catalog.py  OpenRouter 真实模型列表(343+),缓存 1h
  llm.py             OpenRouter 客户端 + JSON 解析健壮性 + 错误脱敏
  observability.py   内存 ring buffer(400 条),完整 prompt/响应可见
  settings.py        运行时设置(模型/温度/闸门阈值)落盘 settings.json
  api.py             FastAPI 路由
web/index.html       暗色单页(账本 + 完整度环 + Log 面板 + 设置弹窗)
tests/               114 passed,FakeLLM 注入,零网络
tests/_offline_check.py   拦截外部主机后重跑,证明零网络依赖
```

## 5. 安全约束(仓库是公开的,这些是硬要求)

- **API key 只存进程内存**(`credentials.py`):不写文件、不进日志、
  不出现在任何接口响应、不进 DOM/localStorage。**重启即失效,需重新输入。**
- 已实测验证:重启后 `has_key: False`;`grep -rl` 磁盘无痕迹;
  假 key 调用返回 `401 User not found`(证明 key 真被使用而非静默忽略)
- **绝不回显 key 任何片段**(连掩码前缀+后四位都不行 —— 可用于关联账号)
- **刻意不扫描 home 目录或其他应用的配置文件**。曾经有过"项目 .env 找不到
  就读 Hermes 的 .env"的逻辑,已删除。有测试
  (`test_source_never_reads_credentials_outside_project`)防止回归,
  且已故意注入回归验证该测试会 FAIL。
- 环境变量 `OPENROUTER_API_KEY` 仍支持(CI 用),但运行时输入优先
- `.env` / `settings.json` / `state.json` / `.dev-state.json` 均已 gitignore

## 6. 已完成 + 已验证的事实(不要重复验证)

**真实 LLM 端到端跑通过。** 用真实议题"如何系统地教别人 AI 产品知识":

第 1 轮(只有 3 条抽象意图)LLM 问出的问题:
> 上一次你试图教某个人 AI 产品知识,他当场问了什么让你卡住的那个点是什么?
> **为什么关键**:你说"教别人最难的是他们没有直觉判断力",但**这是个二手结论**……
> 没有这个,任何"系统教学法"都只是**你脑补出来的理想情况**。

飞轮完整度实测变化:**20% → 45% → 70% → 85%(闸门开启)**

闸门开启后生成的框架带真实 tradeoff 且引用了具体事故:
```
反直觉纠偏框架            基于证据 4 条
  ✓ 防止灾难性决策(如"等100%再上线")
  ✗ 可能让人觉得AI"全是坑"而过度保守
```

**已修掉的真实缺陷(供你理解代码里那些"奇怪"的设计):**
1. `render_context` 曾按枚举声明顺序迭代,导致最贵的 evidence 排最后给 LLM 看 → 改权重降序
2. **闸门条件自相矛盾**(最严重):`SATURATION=30` 时结构最小值只有 43%,永远过不了 60%
3. 闸门与缺口检测器未联动:跑到 70% 时 LLM 连问两轮 evidence,而卡点是"缺 1 条约束"
   → 现在把 `gate.missing` 喂进 prompt
4. 测试曾偷偷打真实 API(耗时 2.3s→3.9s)→ 改假响应 fixture + 加断网校验脚本
5. 设置面板 modal body 434px 但内容 1114px,模型列表落在折叠线下 260px

## 7. 怎么跑起来

```bash
cd /d/Github_PROJECT/kindling
uv sync --extra dev
uv run pytest tests/ -q                  # 预期 114 passed
uv run python tests/_offline_check.py    # 断网校验,预期 114 passed
uv run python -m kindling                # → http://127.0.0.1:8777
```

启动后点右上角**「设置」**粘贴 OpenRouter key(每次重启都要,这是刻意设计)。

**注意**:之前 dogfood 的状态文件 `.dev-state.json` 已随进程清理被删除,
账本现在是空的。用户需要从零开始录入,或你帮他重建种子上下文。

## 8. ★ 唯一真正重要的未完成事项

**核心假设至今只用模拟数据验证过,用户本人还没真实用过一轮。**

那个假设是:**LLM 提问 / 用户回答,比用户自己写 prompt 更能产出高质量上下文。**

需要用户亲自做的验证(计划文档里叫 Task D1),两个指标:

1. **被连续问 3-5 轮之后,体感是「被帮助」还是「被审问」?**
   —— 这决定留存,比任何准确率都重要
2. **闸门开启后的框架,是否明显比他以前直接问 LLM 拿到的更"只对他成立"?**
   —— 这验证"上下文复利"是否真实存在

**判定标准**:LLM 问出的问题里,有 ≥3 个是他自己写 prompt 时**绝不会主动提供**的信息。
若 <3 个 → 整个架构不成立,应停下重新诊断,而不是继续加功能。

**建议的对比实验**(现在模型选择器已就绪):
同一批上下文,用 `anthropic/claude-sonnet-4.5` 和 `anthropic/claude-opus-4.1`
各生成一次框架,看缺口问题的尖锐程度差多少。
如果便宜模型(如 `deepseek/deepseek-chat-v3.1`,$0.25/M)就够用,
这个产品的可推广性完全不同。

## 9. 明确不做(YAGNI 边界,已达成共识)

数据库 / 向量检索 / 多议题 / 协作 / 云同步 / 移动端 / 语音输入 /
自动导入聊天记录 / LLM 打完整度分(会漂移,闸门必须确定性) / 前端框架

**每次想加功能前,先问:这是在验证核心假设,还是在逃避验证?**

## 10. 你接手后该怎么做

1. 先确认用户想干什么 —— 是继续验证假设,还是改功能,还是别的
2. **如果他想加功能,先提醒他核心假设还没验证过**(但别说教,他自己会判断)
3. 改代码前先跑一次测试确认基线
4. 任何涉及 key/凭据的改动,都要保持"只存内存、绝不落盘"这条底线
5. 报告结果时**必须基于真实工具输出**,不要编造。做不到就直说做不到。

参考文档(在原机器上,可能不在新机器):
`E:\AI-Chat\Chat\.hermes\plans\2026-07-26_171111-kindling-v2-context-engine.md`
(v2 完整设计与任务分解;v1 是 `..._155930-kindling-thought-ignition-product.md`)

======================= COPY TO HERE ========================
